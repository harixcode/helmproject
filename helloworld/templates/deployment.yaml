# helloworld/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloworld
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helloworld
  template:
    metadata:
      labels:
        app: helloworld
    spec:
      containers:
        - name: helloworld
          image: harixcode/hello-world-app:latest
          ports:
            - containerPort: 80
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postDocker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postDocker
  template:
    metadata:
      labels:
        app: postDocker
    spec:
      containers:
        - name: postDocker
          image: harixcode/hello-world-app:latest
          ports:
            - containerPort: 80